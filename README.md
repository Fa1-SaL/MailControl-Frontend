# MailControl Frontend

Frontend repository for the MailControl internal email operations console.

This repository currently contains the agreed directory structure only. Application code, dependencies, provider integrations, credentials, and real candidate data have not been added.

The frontend is a presentation and request layer. Authorization, eligibility, suppression, routing, sending, and auditing remain backend responsibilities.
